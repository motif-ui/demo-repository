name: Workflow Protection

on:
  pull_request_target:
    paths:
      - '.github/workflows/**'

jobs:
  check-workflow-changes:
    runs-on: ubuntu-latest
    steps:
      - name: Fail if workflow files changed
        run: |
          echo "❌ Workflow dosyalarında değişiklik tespit edildi!"
          echo "🚫 .github/workflows/ altındaki dosyalar değiştirilemez!"
          exit 1